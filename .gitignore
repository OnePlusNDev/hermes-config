# Sensitive / secret files
.env
.auth*
*.lock
*.pid
gateway_state.json
channel_directory.json

# Secrets store
auth.json

# Runtime / cache artifacts
*.db
*.db-shm
*.db-wal
state.db*
models_dev_cache.json
*.cache
cache/
image_cache/
audio_cache/
context_length_cache.yaml

# Gateway / session state
gateway.lock
session_log.txt

# Large snapshot files
.skills_prompt_snapshot.json
skills_manifest.json

# Cron runtime artifacts (output, tickers)
**/cron/output/
**/cron/ticker_*
**/cron/.gitignore

# Skills internal state (curator, hub, usage)
**/skills/.curator_*
**/skills/.hub/
**/skills/.usage.json
**/skills/.bundled_manifest

# Skip subdirs but keep their .gitignore
skills/.*/
sessions/
logs/
plans/
sandboxes/
pairing/
lsp/
hooks/

# OS / temp files
.DS_Store
*.swp
*.swo
*~
